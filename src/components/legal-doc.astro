---
import type { Locale } from '@/lib/i18n';
import { LEGAL } from '@/data/legal';
import { SECTION_LEAD } from '@/lib/layout';
import { BODY, LEAD, META, TITLE_LG, TITLE_SM } from "@/lib/type";

const { locale } = Astro.props as { locale: Locale };
const doc = LEGAL.doc[locale];
---

<section class="w-full bg-paper text-ink">
  <div class={`mx-auto max-w-3xl ${SECTION_LEAD}`}>
    <h1
      class={`${TITLE_LG} text-ink`}
    >
      {doc.title}
    </h1>
    <p class={`mt-8 max-w-prose ${LEAD} text-ink/70`}>
      {doc.intro}
    </p>

    <div class="mt-16 flex flex-col gap-12 md:mt-24 md:gap-16">
      {
        doc.sections.map((section) => (
          <div class="border-t border-ink/8 pt-8">
            <h2 class={`${TITLE_SM} text-ink`}>
              {section.heading}
            </h2>
            <div class="mt-4 flex flex-col gap-2">
              {section.body.map((line) => (
                <p class={`max-w-prose ${BODY} text-ink/70`}>
                  {line}
                </p>
              ))}
            </div>
          </div>
        ))
      }
    </div>

    <p class={`mt-16 ${META} text-ink/40`}>
      {doc.updated}
    </p>
  </div>
</section>
